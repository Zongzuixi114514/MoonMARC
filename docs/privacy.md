# 隐私扫描说明

`privacy` 命令提供有限的技术辅助：

- 统计 `900-999` 本地字段数量。
- 扫描包含 `@` 和 `.` 的邮箱样式值。
- 扫描至少包含 7 个数字的电话号码样式值。
- `redact --drop-9xx` 删除全部 9XX 字段。
- `redact --mask-email` 将邮箱本地部分替换为 `***`。

这些规则不是个人信息识别模型，也不会覆盖所有姓名、地址、URL、备注或机构内部标识。隐私扫描没有发现匹配项，也不代表数据可以直接公开。正式发布前必须人工审核，并根据数据来源和机构政策进行额外处理。
